↑convertigo: 8.0.0.m006
↓lib_ConvertigoAgentBridge [core.Project]: 
  comment: |
    Local runtime bridge between the Tigo Assistant and workspace-managed OpenAI Codex or Mistral Vibe agents. It manages isolated homes, persistent processes, events, runtime installation, and opaque in-memory MCP credential handles.
    
    Requires Convertigo Studio 8.4.4 or newer when used by the Tigo local Agent stack.
  contextTimeout: 59
  corsOrigin: =Origin
  httpSessionTimeout: 600
  version: 0.4.11
  ↓void [connectors.SqlConnector]: 🗏 connectors/void.yaml
  ↓agent_claude_close [sequences.GenericSequence]: 🗏 sequences/agent_claude_close.yaml
  ↓agent_claude_prompt [sequences.GenericSequence]: 🗏 sequences/agent_claude_prompt.yaml
  ↓agent_claude_setup [sequences.GenericSequence]: 🗏 sequences/agent_claude_setup.yaml
  ↓agent_claude_start [sequences.GenericSequence]: 🗏 sequences/agent_claude_start.yaml
  ↓agent_cleanup_storage [sequences.GenericSequence]: 🗏 sequences/agent_cleanup_storage.yaml
  ↓agent_codex_close [sequences.GenericSequence]: 🗏 sequences/agent_codex_close.yaml
  ↓agent_codex_prompt [sequences.GenericSequence]: 🗏 sequences/agent_codex_prompt.yaml
  ↓agent_codex_setup [sequences.GenericSequence]: 🗏 sequences/agent_codex_setup.yaml
  ↓agent_codex_start [sequences.GenericSequence]: 🗏 sequences/agent_codex_start.yaml
  ↓agent_events [sequences.GenericSequence]: 🗏 sequences/agent_events.yaml
  ↓agent_python_setup [sequences.GenericSequence]: 🗏 sequences/agent_python_setup.yaml
  ↓agent_settings [sequences.GenericSequence]: 🗏 sequences/agent_settings.yaml
  ↓agent_status [sequences.GenericSequence]: 🗏 sequences/agent_status.yaml
  ↓agent_sweep_expired [sequences.GenericSequence]: 🗏 sequences/agent_sweep_expired.yaml
  ↓agent_vibe_close [sequences.GenericSequence]: 🗏 sequences/agent_vibe_close.yaml
  ↓agent_vibe_prompt [sequences.GenericSequence]: 🗏 sequences/agent_vibe_prompt.yaml
  ↓agent_vibe_setup [sequences.GenericSequence]: 🗏 sequences/agent_vibe_setup.yaml
  ↓agent_vibe_start [sequences.GenericSequence]: 🗏 sequences/agent_vibe_start.yaml